apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-mcp-server-proxy
  namespace: mcp-server
  labels:
    app: postgres-mcp-server-proxy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-mcp-server-proxy
  template:
    metadata:
      labels:
        app: postgres-mcp-server-proxy
    spec:
      serviceAccountName: postgres-mcp-server-proxy
      containers:
      # Auth proxy sidecar
      - name: auth-proxy
        image: image-registry.openshift-image-registry.svc:5000/mcp-server/auth-proxy:latest
        ports:
        - containerPort: 8080
          name: public
        # Service account token is automatically mounted at /var/run/secrets/kubernetes.io/serviceaccount
        resources:
          requests:
            memory: "32Mi"
            cpu: "25m"
          limits:
            memory: "128Mi"
            cpu: "100m"
            
      # Main MCP server (unchanged)
      - name: postgres-mcp-server
        image: image-registry.openshift-image-registry.svc:5000/mcp-server/postgres-mcp-server:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
          name: http
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: postgres-mcp-secret
              key: database-url
        - name: PORT
          value: "3000"
        - name: NODE_ENV
          value: "production"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 10

      # No volumes needed - service account token is automatically mounted
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-mcp-server-service-proxy
  namespace: mcp-server
  labels:
    app: postgres-mcp-server-proxy
spec:
  selector:
    app: postgres-mcp-server-proxy
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  type: ClusterIP
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: postgres-mcp-server-route-proxy
  namespace: mcp-server
  labels:
    app: postgres-mcp-server-proxy
spec:
  to:
    kind: Service
    name: postgres-mcp-server-service-proxy
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect